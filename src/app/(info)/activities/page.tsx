import withLayout from '@/hoc/WithLayout'

const Activities: React.FC = () => {
  return <div className='max-w-content w-full mt-4'>여기는 Activities Page입니다</div>
}

export default withLayout(Activities)
